import React from 'react'

class SaveButton extends React.Component{
    render(){
        return(
            <div>
                <button onClick={()=>}>Save User</button>
            </div>
        )
    }
}

export default SaveButton